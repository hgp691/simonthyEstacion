class TyH:
        def __init__(self,fecha,hora,temperatura,humedad):
                self.fecha=fecha
                self.hora=hora
                self.temperatura=temperatura
                self.humedad=humedad
